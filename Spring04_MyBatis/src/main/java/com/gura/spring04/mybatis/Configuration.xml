<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd"><!-- 이거때문에 여기의 형식이 정해지는 것 -->
<configuration>
	
	<!-- 사용할 Mapper 목록 설정 --><!-- servlet-context.xml 에 SqlSessionFactory 객체에서 사용한다. -->
	<mappers>
		<mapper resource="com/gura/spring04/mybatis/MemberMapper.xml"/>
	</mappers>
</configuration>