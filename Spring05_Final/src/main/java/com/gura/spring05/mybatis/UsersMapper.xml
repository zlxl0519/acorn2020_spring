<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<<<<<<< HEAD
<mapper namespace="users"><!-- java.lang.String 을 string 이라고 적어도 된다. -->
	<select id="isExist" parameterType="java.lang.String" resultType="string">
		select id
		from users
		where id=#{inputId}
	</select>
	<insert id="insert" parameterType="usersDto">
		insert into users
		(id, pwd, email, regdate)
		values(#{id}, #{pwd}, #{email}, sysdate)
	</insert>
	<select id="getData" parameterType="string" resultType="usersDto"> <!-- 여기의 regdate가 dto 의 필드명과 같아야 된다. -->
		select id, pwd, email, profile, to_char(regdate,'yy.mm.dd hh24"시"mm"분"')as regdate
		from users
		where id=#{id}
	</select>
	<delete id="delete" parameterType="string">
		delete from users
		where id=#{id}
	</delete>
	<update id="update" parameterType="usersDto">
		update users
		set email=#{email}, profile=#{profile}
		where id=#{id}
	</update>
	<update id="updatePwd" parameterType="usersDto">
		update users
		set pwd=#{newPwd}
		where id=#{id}
	</update>
</mapper>
=======
<mapper namespace="users">
	<select id="isExist" parameterType="java.lang.String" 
		resultType="string">
		SELECT id 
		FROM users
		WHERE id=#{inputId}
	</select>
	<insert id="insert" parameterType="usersDto">
		INSERT INTO users
		(id, pwd, email, regdate)
		VALUES(#{id}, #{pwd}, #{email}, SYSDATE)
	</insert>
	
	<select id="getData" parameterType="string"
		resultType="usersDto">
		SELECT id,pwd,email,profile,
			TO_CHAR(regdate, 'YY.MM.DD HH24"시"MM"분"') AS regdate
		FROM users
		WHERE id=#{id}	
	</select>
	
	<delete id="delete" parameterType="string">
		DELETE FROM users
		WHERE id=#{id}
	</delete>
	
	<update id="update" parameterType="usersDto">
		UPDATE users
		SET email=#{email},profile=#{profile}
		WHERE id=#{id}
	</update>
	
	<update id="updatePwd" parameterType="usersDto">
		UPDATE users
		SET pwd=#{newPwd}
		WHERE id=#{id}
	</update>
</mapper>










>>>>>>> refs/remotes/upstream/master
