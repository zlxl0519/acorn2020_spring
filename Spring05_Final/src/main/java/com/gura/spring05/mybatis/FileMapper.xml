<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="file">
	<select id="getList" parameterType="fileDto" resultType="fileDto">
		SELECT *
		FROM
			(SELECT result1.*, ROWNUM AS rnum
			FROM
				(SELECT num, writer, title, orgFileName, saveFileName, fileSize, regdate
				FROM board_file
				<choose>
					<when test="title != null and orgFileName != null"><!-- 파라미터 타입으로 전달된 데이터를 읽을수 있다. -->
						WHERE title LIKE '%${title}%' <!-- 전처리문이라고 한다.(실행전에 미리 구성해주는 것) -->
						OR orgFileName LIKE '%'||#{orgFileName}||'%'
					</when>
					<when test="title != null"><!-- #{} 은 ?로 나타내서 바인드 하면서 실행할때 값을 넣어준다. ${}보다 빠르다. -->
						WHERE title LIKE '%'||#{title}||'%'
					</when>
					<when test="writer != null"><!-- '${}'은 실행하기 전에 미리 문자열을 구성하는 역할  ${}은 숫자를 구성할때-->
						WHERE writer LIKE '%${writer}%'
					</when>
				</choose>
				ORDER BY num DESC) result1)
		WHERE rnum BETWEEN #{startRowNum} AND #{endRowNum}
	</select>
	
	<select id="getCount" parameterType="fileDto" resultType="int">
		SELECT NVL(MAX(ROWNUM), 0)
		FROM board_file
		<choose>
			<when test="title != null and orgFileName != null"><!-- 파라미터 타입으로 전달된 데이터를 읽을수 있다. -->
				WHERE title LIKE '%${title}%' <!-- 전처리문이라고 한다.(실행전에 미리 구성해주는 것) -->
				OR orgFileName LIKE '%'||#{orgFileName}||'%'
			</when>
			<when test="title != null"><!-- #{} 은 ?로 나타내서 바인드 하면서 실행할때 값을 넣어준다. ${}보다 빠르다. -->
				WHERE title LIKE '%'||#{title}||'%'
			</when>
			<when test="writer != null"><!-- '${}'은 실행하기 전에 미리 문자열을 구성하는 역할  ${}은 숫자를 구성할때-->
				WHERE writer LIKE '%${writer}%'
			</when>
		</choose>
	</select>
	
	<insert id="insert" parameterType="fileDto">
		INSERT INTO board_file
		(num, writer, title, orgFileName, saveFileName, fileSize, regdate)
		values(board_file_seq.NEXTVAL, #{writer}, #{title}, 
			#{orgFileName}, #{saveFileName}, #{fileSize}, SYSDATE)
	</insert>
	<select id="getData" parameterType="int" resultType="fileDto">
		SELECT num, writer, title, orgFileName, saveFileName, fileSize, regdate
		FROM board_file
		WHERE num=#{num}
	</select>
	
	<delete id="delete" parameterType="int">
		DELETE FROM board_file
		WHERE num=#{num}
	</delete>
</mapper>